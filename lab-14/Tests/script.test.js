const sayHi = require("../script");
test("Say hi Jest Message", () => {
 expect(sayHi()).toBe("Hi, we start learning Jest!");
});
test("Say hi Jest Message", () => {
    expect(sayHi()).toBe("Hi, we start learning Jest!");
   });